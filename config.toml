[theme]
dark = "kanabox_default"
light = "edge_light"
fallback = "kanabox_default"

[editor]
line-number = "relative"
end-of-line-diagnostics = "hint"

[editor.inline-diagnostics]
cursor-line = "warning"
